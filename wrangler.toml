# Cloudflare Workers configuration (used only for local preview or manual deploy)
# Official docs: https://opennext.js.org/cloudflare

name = "infinite-monkey-theorem"
main = ".open-next/worker.js"
compatibility_date = "2026-01-01"
compatibility_flags = ["nodejs_compat"]

# Static asset binding generated by OpenNext
assets = { directory = ".open-next/assets", binding = "ASSETS" }

# Environment variables are injected from the build-generated .env